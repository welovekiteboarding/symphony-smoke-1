# symphony-smoke-1 Status

## Goal

Build `symphony-smoke-1` through the Symphony Linear -> Codex -> GitHub loop.

## Graph Smoke Proof

The `SM1` Linear team is using the planning graph at `planning/graph.json` to drive this scaffolded smoke run.

Graph task `smoke-graph-001` records a tiny, factual proof point so the repo can demonstrate graph materialization, worker execution, review, merge, and checkpointing without adding product complexity.
